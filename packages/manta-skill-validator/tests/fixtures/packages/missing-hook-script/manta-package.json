{
  "schemaVersion": 1,
  "name": "@manta-library/missing-hook-script",
  "version": "1.0.0",
  "description": "Fixture variant where the manifest declares a hook whose script file is missing.",
  "author": "Manta test fixture",
  "license": "MIT",
  "mantaVersionCompat": ">=0.7.0 <1.0.0",
  "contributes": {
    "skills": [
      {
        "name": "sample-skill",
        "description": "Sample skill in the missing-hook-script fixture variant."
      }
    ],
    "modes": [
      {
        "name": "sample-mode",
        "description": "Sample mode in the missing-hook-script fixture variant.",
        "basedOn": "recon-swarm",
        "cloneCount": { "min": 1, "max": 1 },
        "sessionMode": "batch"
      }
    ],
    "hooks": [
      {
        "event": "PreToolUse",
        "script": "hooks/missing.sh",
        "requiresApproval": true
      }
    ]
  }
}
