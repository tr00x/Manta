# Last-gasp report — clone-A — RB2 Chunk 1 (package identity + publish metadata)

cast: `cast-1780081439432` · mode: forking-realities · branch: `manta/cast-1780081439432/A`

## Summary

Made `packages/manta-cli` nominally publishable as the **unscoped npm package `manta@0.1.0`** —
NO bundling (that is Chunk 2). The npm **name** changed (`@manta/cli` → `manta`); the **directory**
`packages/manta-cli/` did NOT change, so all `dist/bin/manta.cjs` filesystem-path constants in the e2e
suite are untouched by design. One atomic commit. All gates real and green.

## Files changed (11 tracked)

1. `packages/manta-cli/package.json` — `name` `@manta/cli`→`manta`; removed `"private": true`;
   `version` `0.0.0`→`0.1.0`; added `engines.node ">=20"`, `"prepublishOnly": "pnpm run build"`,
   `repository`/`homepage`/`keywords`. Kept `files:["dist"]` and `bin.manta:"./dist/bin/manta.js"`.
2. `packages/manta-cli/src/bin/manta.ts` — `.version('0.0.0')` → `.version('0.1.0')` (the literal
   `manta --version` output), line 145.
3. `packages/manta-cli/src/library/cli-version.ts` — `MANTA_CLI_VERSION = '0.0.0'` → `'0.1.0'`
   (feeds library-install compat check), line 9.
4. `packages/manta-cli/tsconfig.json` — added `{ "path": "../manta-skill-validator" }` to
   `references` (cli imports skill-validator; `tsc -b` needs the project ref).
5. `packages/manta-e2e/package.json` — dep `"@manta/cli": "workspace:*"` → `"manta": "workspace:*"`.
6. `packages/manta-e2e/tests/manta-library.e2e.test.ts` — `--filter '@manta/cli...'` → `--filter 'manta...'`
   (+ error-message text), so the e2e build step still matches a package.
7. `packages/manta-e2e/tests/transcript-inheritance.e2e.test.ts` — `import('@manta/cli')` → `import('manta')`
   (+ one code comment referencing the package name).
8. `packages/manta-cli/README.md` — package-local doc: header `# @manta/cli`→`# manta`,
   `pnpm --filter @manta/cli build`→`pnpm --filter manta build`, `from '@manta/cli'`→`from 'manta'`.
   (npm always ships README in the tarball regardless of `files`, so the stale name would have shown
   on the npm page — fixed as part of package identity.)
9. `packages/manta-cli/ARCHITECTURE.md` — package-local doc: header + one `@manta/cli` consumer ref
   renamed (other `@manta/bus`/`@manta/orchestrator`/`@manta/snapshot` refs left intact — those
   packages are NOT renamed in this chunk).
10. `CHANGELOG.md` — folded the entire `[Unreleased]` block (Phases 7b/7a/2) INTO the existing
    `[0.1.0]` section, re-dated the single heading to `## [0.1.0] — 2026-05-29`, added a first-publish
    intro paragraph. No duplicate `[0.1.0]` heading. Stale **"18 tools"** corrected to the REAL count
    **"25 tools"** (counted from `packages/manta-bus/src/server.ts`: 25 `name: 'manta.*'` tool defs —
    register, heartbeat, suicide_intent, report_death, task_contract.write/read, ack_contract,
    contract_refresh, claim_work, release_work, lock, unlock, renew_lock, broadcast, message,
    drift_report, read_broadcasts, zk_write, para_append, retask, pause, resume, request_task,
    feedback, enqueue_work).
11. `pnpm-lock.yaml` — regenerated by `pnpm install`; diff is exactly the e2e dep rename
    (`@manta/cli` → `manta`, alphabetically resorted). 3 insertions / 3 deletions, no other churn.

## The THREE version sites (all bumped to 0.1.0 in this commit)

`package.json#version` · `src/bin/manta.ts:145` `.version()` · `src/library/cli-version.ts:9`
`MANTA_CLI_VERSION`. Post-edit `grep -rn "0\.0\.0" packages/manta-cli/src packages/manta-cli/package.json`
→ **ZERO hits**.

## Bin-format decision (documented per task §3)

Published `bin.manta = ./dist/bin/manta.js` (ESM) — kept, this is the entry a user gets. The dev e2e
suite STAYS on `dist/bin/manta.cjs` (it tests the dev monorepo build). The Chunk-4 install-from-tarball
e2e (later) exercises the published `.js`. NOT migrating e2e to `.js` in this chunk.

## GATE — real terminal output

**a. `pnpm install` (in clone-A worktree)** — clean; `manta` workspace dep resolves. Pre-build bin
warnings (`dist/...` ENOENT) are expected before `build` and disappear after it.
```
Done in 3.8s
```

**b. `pnpm --filter manta build`** (deps built topologically first via `pnpm -r build` in the fresh
worktree — DTS needs `@manta/bus`/`@manta/orchestrator` .d.ts):
```
DTS ⚡️ Build success in 2024ms
DTS dist/index.d.ts  51.53 KB
DTS dist/index.d.cts 51.53 KB
```

**c. `node packages/manta-cli/dist/bin/manta.cjs --version`**:
```
0.1.0
```

**d. `cd packages/manta-cli && npm publish --dry-run`** (prepublishOnly ran the build, then):
```
npm notice name: manta
npm notice version: 0.1.0
npm notice filename: manta-0.1.0.tgz
npm notice total files: 12   (README.md + dist/* + package.json; NOT private)
+ manta@0.1.0
EXIT=0
```
(Benign npm warning `"bin[manta]" script name was cleaned` — npm normalizes bin entries; non-fatal,
name/version/access all correct.)

**e. `grep -rn "0\.0\.0" packages/manta-cli/src packages/manta-cli/package.json`**:
```
ZERO hits (good)
```

**f. `pnpm gate` (typecheck + lint + test, fail-fast)** — `REAL_GATE_EXIT=0`:
```
 Test Files  169 passed (169)
      Tests  1440 passed (1440)
   Duration  67.32s
```

**residual check** — `git grep -n '@manta/cli' -- packages` → **NONE**.

## Pending / out-of-scope (handed to later chunks)

- Bundling, and bus+validator dep/bin changes — **Chunk 2** (explicitly NOT touched here).
- Install-from-tarball acceptance e2e against the published `.js` — **Chunk 4**.
- `.manta/heartbeat-touch.cjs` is untracked in the worktree but was present at session start and lives
  under `.manta/` (gitignored / forbidden) — NOT mine, NOT committed.

## Self-certainty: 9/10

Surgical, fully grep-verified rename + version bump; every gate run for real with pasted output; zero
residual refs; lockfile diff is exactly the rename. −1 only for the cosmetic npm `bin script name was
cleaned` warning, which is npm-side normalization and does not affect the published artifact.
