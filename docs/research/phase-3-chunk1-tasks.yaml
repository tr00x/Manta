A:
  task: >-
    Implement Phase 3 Chunk 1 tasks 1.1 through 1.5 from the plan at
    docs/superpowers/plans/2026-05-26-phase-3-charge-system.md.

    Tasks:
    1.1 — BusPaths extension (charges, chargesLog, dailySpend, configDir, budgetConfig)
    1.2 — Charge schemas + MODE_CHARGE_COST constant
    1.3 — ChargeStateSchema + ChargeEventSchema + ChargeEventTypeSchema
    1.4 — DailySpendSchema + DailySpendEntrySchema
    1.5 — BudgetConfigSchema

    Read the plan carefully for exact field names, types, and file paths.
    Use TDD: write failing tests first, then implement.
    Commit each task atomically with conventional commit messages.
    Use git -c user.email="tr00x@proton.me" -c user.name="Tim Hunt".
  approach_hint: >-
    Start by reading the plan Chunk 1 section. Then implement sequentially:
    paths first (1.1), then schemas (1.2-1.5).
    Run pnpm --filter @manta/bus test after each task.

B:
  task: >-
    Implement Phase 3 Chunk 1 tasks 1.6 through 1.10 from the plan at
    docs/superpowers/plans/2026-05-26-phase-3-charge-system.md.

    Tasks:
    1.6 — ChargeStore class (the main store with deductForCast, creditForOutcome, applyPassiveRecovery, refresh, triggerCooldown)
    1.7 — DailySpendLedger class
    1.8 — CastOutcomeClassifier
    1.9 — Wire ChargeStore + DailySpendLedger into BusContext
    1.10 — Extend CliErrorKind + full sweep

    IMPORTANT: Tasks 1.6 and 1.7 depend on schemas from tasks 1.1-1.5 which
    Clone A is implementing in parallel. If schemas don't exist yet, READ THE
    PLAN for the exact schema definitions and create them yourself — the plan
    has full Zod definitions. Don't wait.

    Read the plan carefully for exact field names, types, and file paths.
    Use TDD: write failing tests first, then implement.
    Commit each task atomically with conventional commit messages.
    Use git -c user.email="tr00x@proton.me" -c user.name="Tim Hunt".
  approach_hint: >-
    Start by reading the plan Chunk 1 section (tasks 1.6-1.10).
    Then read tasks 1.1-1.5 for the schemas you'll depend on.
    If Clone A hasn't committed schemas yet, implement them as part of 1.6.
    ChargeStore is the biggest task (~200 LOC + ~350 LOC tests).
    Run pnpm --filter @manta/bus test after each task.
