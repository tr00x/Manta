A:
  task: |
    Draft docs/superpowers/plans/2026-05-28-phase-7b-manta-share.md based
    on docs/research/phase-7-community-share-trust.md (full 690 lines —
    read it all). Format template: docs/superpowers/plans/2026-05-28-phase-7a-manta-library.md.
    Spec anchors: docs/superpowers/specs/2026-05-06-manta-pattern-design.md
    Sec 11.1 line 486 (/manta share), Sec 12 lines 542-543, Sec 15.1 line 644.

    Scope of YOUR plan:
      - /manta share <cast-id> bundle generation (the CLI command)
      - Bundle anatomy: manifest.json + sanitized snapshot/task-contract/
        post-mortems/ZK/events/worktree-diff (per research §1)
      - FULL sanitization enumeration (20+ rules per research §1.4 + audit
        bug #29 lessons): every free-form field across every artifact
      - Bundle integrity: hash of bundle, manifest-vs-disk verification
      - --publish flow to npm with the MVTS-7 threat-model gates (research §0)
      - Reuses Phase 7a's MantaPackageManifest, sanitizer module, lockfile

    Chunked plan (2-3 chunks), executable, file:line citations for every
    existing-code reference (use grep to verify — cross-plan field drift is
    the #1 blocker class per CLAUDE.md). Honest about deferred items: signing/
    reputation/sandbox are Phase 8+. Document the threat model precisely.

    Coordinate with sibling B (drafting Phase 7c) via manta.broadcast for
    cross-cutting concerns — particularly: can a triggered cast auto-share?
    If yes, that contract goes in YOUR plan. Bug #51 fix means object
    payloads in broadcasts now work; read sibling's broadcasts before
    writing your integration section.

    End with a "Verification" section listing the gate commands a reviewer
    would run before approving.

    DO NOT actually implement Phase 7b — your deliverable is the PLAN file
    only. No source-code edits; no test edits.
  approach_hint: |
    Bias toward Phase-7 trust-boundary honesty per research §0: ship
    informed-consent + best-effort static-scan, NOT real security.
    Document threat surface precisely. If something needs Phase 8+ infra,
    say "DEFERRED to Phase 8+" with one-sentence reason.

B:
  task: |
    Draft docs/superpowers/plans/2026-05-28-phase-7c-auto-triggers.md based
    on docs/research/phase-7-auto-cast-triggers.md (full 1015 lines — read
    it all). Format template: docs/superpowers/plans/2026-05-28-phase-7a-manta-library.md.
    Spec anchors: docs/superpowers/specs/2026-05-06-manta-pattern-design.md
    Sec 11.1 line 470, Sec 12 lines 540-541.

    Scope of YOUR plan:
      - /manta trigger add/list/arm/fire/remove CLI surface
      - Trigger event sources for Phase 7 (per research §1.2): (a) git hooks,
        (c) Claude Code hooks, (f) manual fire ONLY. DEFER filesystem
        watchers, test-runner watchers, CI webhooks to Phase 8+.
      - Trigger config storage: .manta/triggers/<name>.yaml (user-tracked)
        + .manta/state/triggers/fires.jsonl (bus-side, gitignored, append-only)
      - Default-disarmed posture; explicit `arm <name>` with mandatory
        --dry-fire pass before the trigger is marked armed
      - Loop detection: widen CastManifestSchema with triggered_by +
        cause_chain. Walk cause_chain before every trigger-fired spawn;
        refuse if depth > 3 or same trigger appears twice in chain.
      - Budget gate: EVERY trigger-fired cast goes through runPreSpawnGate.
        No bypass flag. Per-trigger hourly USD cap is an extra gate.

    Chunked plan, executable, file:line citations, no TODOs. The headline
    residual risk (user-authored trigger DSL) must be called out explicitly
    in a "Risk surface" section.

    Coordinate with sibling A (drafting Phase 7b) — particularly: if
    triggers can auto-share, that interaction lives in A's plan but A
    needs YOUR contract for `triggered_by` metadata propagation. Broadcast
    your contract.

    End with a "Verification" section.

    DO NOT actually implement Phase 7c — your deliverable is the PLAN file
    only. No source-code edits; no test edits.
  approach_hint: |
    Bias toward saying "no" by default per research §0 — every guardrail
    spelled out BEFORE any happy-path code. This is the highest-risk
    feature in Phase 7. The arm-with-dry-fire posture is non-negotiable.
