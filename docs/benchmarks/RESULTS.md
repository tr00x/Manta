# Benchmark results

> ⚠️ **UNMEASURED — TEMPLATE ONLY.** Every cell below is `— (run to fill)`. No
> benchmark has been run yet. These tables are placeholders the harness fills in.
> Do **not** cite any value here as a result; there are none. See
> [`README.md`](./README.md) for the method.

## Environment

Fill this in whenever you record results — numbers are meaningless without it.

| Field | Value |
|---|---|
| Date | — (run to fill) |
| Machine (CPU / RAM / OS) | — (run to fill) |
| `claude --version` | — (run to fill) |
| Model | — (run to fill) |
| Repeats per cell | — (run to fill, ≥ 3) |

## Pass-rate (lead with this)

How often each method produced a result that passes the task's success check.
`p/n` = passes / repeats.

| Task | manta | subagents | solo |
|---|---|---|---|
| `recon-map` | — (run to fill) | — (run to fill) | — (run to fill) |
| `refactor-rename` | — (run to fill) | — (run to fill) | — (run to fill) |
| `bug-multilayer` | — (run to fill) | — (run to fill) | — (run to fill) |

## Wall-clock — median seconds (passing runs only)

| Task | manta | subagents | solo |
|---|---|---|---|
| `recon-map` | — (run to fill) | — (run to fill) | — (run to fill) |
| `refactor-rename` | — (run to fill) | — (run to fill) | — (run to fill) |
| `bug-multilayer` | — (run to fill) | — (run to fill) | — (run to fill) |

## Diff size — median lines changed (passing runs only)

| Task | manta | subagents | solo |
|---|---|---|---|
| `recon-map` | — (n/a, read-only) | — (n/a, read-only) | — (n/a, read-only) |
| `refactor-rename` | — (run to fill) | — (run to fill) | — (run to fill) |
| `bug-multilayer` | — (run to fill) | — (run to fill) | — (run to fill) |

## Usage — median token-estimate (passing runs only)

| Task | manta | subagents | solo |
|---|---|---|---|
| `recon-map` | — (run to fill) | — (run to fill) | — (run to fill) |
| `refactor-rename` | — (run to fill) | — (run to fill) | — (run to fill) |
| `bug-multilayer` | — (run to fill) | — (run to fill) | — (run to fill) |

## Notes / observations

Record qualitative findings here as you run (e.g. "manta clone inherited the
constraint discussed pre-cast; the cold subagent re-derived and got it wrong").
Keep them honest — including cases where Manta lost.

- — (run to fill)
